{
  "name": "aprograma",
  "version": "1.0.0",
  "description": "aprograma",
  "contributors": [
    {
      "name": "Daniele Falchetti <danielefalche@gmail.com>"
    },
    {
      "name": "aprograma"
    }
  ],
  "private": true,
  "scripts": {
    "push:dev": "git checkout develop && git add -A && git commit -m 'develop' && git push origin develop",
    "push:prod": "git checkout develop && git fetch origin master && git merge master && git push origin develop:master && git fetch origin master:master",
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/sitemap": "^2.4.0",
    "core-js": "^3.9.1",
    "dompurify": "^2.2.7",
    "marked": "^2.0.1",
    "nuxt": "^2.15.3",
    "nuxt-compress": "^4.1.1",
    "storyblok-nuxt": "^1.3.1"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config": "^6.0.0",
    "@nuxtjs/tailwindcss": "^4.0.2",
    "eslint": "^7.23.0"
  }
}
